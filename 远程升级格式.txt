裸机：  
	start:

  	等待远程升级指令：	

  			上位机待命		有IP登录后等待接收指令
  			读取到指令
  					0x12 0x12 0x00	0x24 
  			开始发送数据

  	发送数据：

  			下位机待命
        delay5s
  			上位机发送数据包
  		头帧				    长度					         地址				          序号									            数据				  校验
  		俩个字节			   	俩个字节	         	        四个字节（累计加）			一个字节								          	512字节			一个字节累计加
  		0x11 0x11 		 	1024      			       0x8004000				    0x01（累计加，第一次为0x01）			512字节			累计加


  	接收完成返回：
  			上位机待命
  			下位机发送指令表示已完成接收写flash，等待接收下一包
  			上位机读取
  					头帧				  序号						         校验
  					俩个字节			对应上位机发送的序号		 一个字节累计加
  					0x12 0x12		0x01 					         累计加

  	：
  	：
  	：
  	：
  	数据长度不够以实际为准;
    delay5s
  	全部发送完成等待最后一包接收完成报文后，发送结束重启指令
  			头帧			      命令			    校验
  			0x11 0x11     0x11        0x33


  	end

 每次重启检测是否更新：
 	start：	
 		发送指令：
 		下位机发送
 			是否有更新
 			头帧				命令			校验
 			0x13 0x13		0x88		0xAE
 			发送后等待10秒是否有返回
 				有返回
 				执行裸机程序，开始烧录
 				无返回
 				执行APP程序
 		上位机返回数据
 			有更新
 			头帧				命令			校验
 			0x11 0x11       0x12		0x34
 			无更新
 			不返回数据
 	end
运行中更新程序：
	start:
		上位机发送：
			MQTT格式发送：
			头帧			    ID号				命令		校验
			0x88 0x99   （四个字节）			0x03	一个字节（累计加）
		下位机接收：
			重启
			执行裸机程序

